# Ignore Git and IDE specific files
.git/
.gitignore
.idea/
.vscode/

# Ignore Python virtual environment and cache
venv/
.env/
__pycache__/
*.pyc
*.pyo
*.pyd

# Ignore local development artifacts and data
mlruns/
mlartifacts/
*.DS_Store
*.log

# Ignore local model files (they will be generated during the build if needed)
# In a production setup, these would be pulled from a model registry.
# For this project, we assume the model is part of the build context
# but we can still ignore any locally generated ones to be safe.
models/
*.pkl